The builds directory exists to hold old built copies of the package.  You can rm the other files in this
directory if you wish.  The README file causes git to build the directory so the scripts work correctly.
